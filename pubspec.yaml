name: task_cli
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.10.4

# Add regular dependencies here.
dependencies:
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
